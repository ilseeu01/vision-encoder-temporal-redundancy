# env: qwen3vl  (Python 3.11)  — CLIP, DINOv2, Qwen2.5-VL, Qwen3-VL, LLaVA-OneVision
# torch is a CUDA build: install from the matching index, e.g.
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
transformers==4.57.1
accelerate==1.13.0
safetensors==0.7.0
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
pillow==12.2.0
av==17.1.0
yt-dlp==2026.6.9        # only for dl_videos.sh
# decord is optional here (not installed in this env); loader falls back to PyAV (av)
